import { Box } from "@mui/material";

const FormatIOIcon = ({ children }: { children: React.ReactNode }) => {
  return <Box alignSelf={{ xs: "flex-start", sm: "center" }}>{children}</Box>;
};

export default FormatIOIcon;
